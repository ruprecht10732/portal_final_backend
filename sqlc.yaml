version: "2"
sql:
  # Auth Domain – users, tokens, roles
  - schema: "migrations"
    queries: "internal/auth/sql"
    engine: "postgresql"
    gen:
      go:
        package: "authdb"
        out: "internal/auth/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Leads Domain – leads, services, activity, notes, AI analysis
  - schema: "migrations"
    queries: "internal/leads/sql"
    engine: "postgresql"
    gen:
      go:
        package: "leadsdb"
        out: "internal/leads/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Catalog Domain – products, VAT rates, materials, assets
  - schema: "migrations"
    queries: "internal/catalog/sql"
    engine: "postgresql"
    gen:
      go:
        package: "catalogdb"
        out: "internal/catalog/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Exports Domain – Google Ads export credentials and conversion events
  - schema: "migrations"
    queries: "internal/exports/sql"
    engine: "postgresql"
    gen:
      go:
        package: "exportsdb"
        out: "internal/exports/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Appointments Domain – appointments, availability, visit reports, attachments
  - schema: "migrations"
    queries: "internal/appointments/sql"
    engine: "postgresql"
    gen:
      go:
        package: "appointmentsdb"
        out: "internal/appointments/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Services Domain – service types
  - schema: "migrations"
    queries: "internal/services/sql"
    engine: "postgresql"
    gen:
      go:
        package: "servicesdb"
        out: "internal/services/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Search Domain – global full-text search
  - schema: "migrations"
    queries: "internal/search/sql"
    engine: "postgresql"
    gen:
      go:
        package: "searchdb"
        out: "internal/search/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # ISDE Domain – subsidy calculation rule tables and meldcode lookup
  - schema: "migrations"
    queries: "internal/isde/sql"
    engine: "postgresql"
    gen:
      go:
        package: "isdedb"
        out: "internal/isde/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Notification Domain – notification module lookup queries
  - schema: "migrations"
    queries: "internal/notification/sql"
    engine: "postgresql"
    gen:
      go:
        package: "notificationdb"
        out: "internal/notification/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Webhook Domain – webhook API keys, Google configs, lead metadata
  - schema: "migrations"
    queries: "internal/webhook/sql"
    engine: "postgresql"
    gen:
      go:
        package: "webhookdb"
        out: "internal/webhook/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # IMAP Domain – user IMAP accounts and synced message metadata
  - schema: "migrations"
    queries: "internal/imap/sql"
    engine: "postgresql"
    gen:
      go:
        package: "imapdb"
        out: "internal/imap/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Partners Domain – partners, invites, service types, and offers
  - schema: "docs/baseline_schema.sql"
    queries: "internal/partners/sql"
    engine: "postgresql"
    gen:
      go:
        package: "partnersdb"
        out: "internal/partners/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Identity Domain – organizations, settings, invites, and workflow engine
  - schema: "migrations"
    queries: "internal/identity/sql"
    engine: "postgresql"
    gen:
      go:
        package: "identitydb"
        out: "internal/identity/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Quotes Domain – quotes, exports, integrations, jobs, and feedback
  - schema: "migrations"
    queries: "internal/quotes/sql"
    engine: "postgresql"
    gen:
      go:
        package: "quotesdb"
        out: "internal/quotes/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # WhatsApp Agent Domain – autonomous WhatsApp agent users, invite codes, and messages
  - schema: "migrations"
    queries: "internal/whatsappagent/sql"
    engine: "postgresql"
    gen:
      go:
        package: "whatsappagentdb"
        out: "internal/whatsappagent/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

  # Memory Domain - semantic memory for agents
  - schema: "migrations"
    queries: "platform/adk/memory/sql"
    engine: "postgresql"
    gen:
      go:
        package: "memorydb"
        out: "platform/adk/memory/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false

